# No [build-system], no [project] -> uv won't try to build your code.
# It will only create a venv and install these dependencies.

[tool.uv]
managed = true
default-groups = ["deps"]

required-environments = [
    "sys_platform == 'win32' and platform_machine == 'AMD64'",
]

[dependency-groups]
deps = [
  # Core
  "numpy",
  "scipy",
  "dill",
  "pandas",
  "numba",
  "pyteomics",
  "pymzml",
  "brain-isotopic-distribution",
  "biosaur2",
  "lxml",
  "regex",
  "tqdm",
  "peppy_sage",

  # Visualization
  "matplotlib",
  "seaborn",
  "plotly",
  "matplotlib-venn",
  "upsetplot",
  "pillow",

  # Data engineering / IO
  "pyarrow",
  "polars",

  # Classical ML / stats
  "scikit-learn",
  "xgboost",
  "statsmodels",
  "joblib",

  # Optimization / math extras
  "cvxopt",
  "ptinnls",

  # Profiling & perf tools
  "line-profiler",
  "memory-profiler",
  "pyinstrument",

  # GUI (Qt + optional Tk themes)
  "PySide6",
  "shiboken6",
  "sv-ttk",
  "ttkthemes",

  # OS-specific helper
  "pywin32; platform_system == 'Windows'",

  # TensorFlow per platform
  # Windows x64 only (officially supported TF wheel)
  "tensorflow-intel; sys_platform == 'win32' and platform_machine == 'AMD64'",

  # Apple Silicon (arm64 macOS) → Apple TF + Metal plugin
  "tensorflow-macos; platform_system == 'Darwin' and platform_machine == 'arm64'",
  "tensorflow-metal; platform_system == 'Darwin' and platform_machine == 'arm64'",

  # Intel macOS → CPU TF fallback (no Metal)
  "tensorflow; platform_system == 'Darwin' and platform_machine != 'arm64'",

  # Linux → standard TF (CPU or NVIDIA GPU runtime if CUDA libs present)
  "tensorflow; platform_system == 'Linux'",


  # Shared TF tooling
  "tensorboard",
  "tensorboard-data-server",
  "tensorflow-io-gcs-filesystem",
]
